server:
  port: 8087

spring:
  application:
    name: intercambioMS
  
  datasource:
    url: jdbc:mysql://localhost:3306/intercambio_db
    username: root
    password: ""
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
