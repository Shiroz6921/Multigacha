server:
  port: 8083

spring:
  application:
    name: clientesMS
  datasource:
    url: jdbc:mysql://localhost:3306/clientes_db
    username: root
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/