server:
  port: 8088

spring:
  application:
    name: compraMS
  
  datasource:
    url: jdbc:mysql://localhost:3306/compra_db
    username: root
    password: ""
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
