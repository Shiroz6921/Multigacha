server:
  port: 8089

spring:
  application:
    name: catalogoMS
  
  datasource:
    url: jdbc:mysql://localhost:3306/catalogo_db
    username: root
    password: ""
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
